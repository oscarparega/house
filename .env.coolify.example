# Configure these values in the Coolify application environment.
# Use a URL-safe password because it is embedded in DATABASE_URL.

POSTGRES_USER=house
POSTGRES_PASSWORD=replace-with-a-long-random-url-safe-password
POSTGRES_DB=house
